"""DIALECT's command-line interface (Typer).

A thin shim over :mod:`dialect.api` (and :mod:`dialect.experiments` for the
simulation commands): it parses flags and delegates; it holds no domain logic.
"""
