"""Plotting for DIALECT: networks, simulation curves, distributions, summary tables.

The visualization layer reads cohort data (``data``) and result/statistics frames
(``stats``) and renders figures. It is imported by ``api``/``cli`` and the research
scripts in ``analysis/``; it never imports ``bmr``, ``baselines``, ``api``, or ``cli``.
"""
